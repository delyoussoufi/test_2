import { ReplaceWhitePipe } from './replace-white.pipe';

describe('ReplaceWhitePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceWhitePipe();
    expect(pipe).toBeTruthy();
  });
});
